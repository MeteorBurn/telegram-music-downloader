# 🚀 Concurrent Downloads Feature

## Обзор

Новая функциональность многопоточного скачивания позволяет загружать несколько файлов одновременно, значительно ускоряя процесс скачивания музыки из Telegram каналов.

## ✨ Новые возможности

### 🔄 Многопоточное скачивание
- **Параллельная загрузка**: До 3-5 файлов одновременно (настраивается)
- **Умная очередь**: Приоритизация файлов по размеру
- **Rate limiting**: Соблюдение лимитов Telegram API
- **Отказоустойчивость**: Автоматические повторы при ошибках

### 📊 Мониторинг в реальном времени
- **Прогресс-бар**: Визуальное отображение прогресса
- **Статистика воркеров**: Информация о каждом потоке скачивания
- **Скорость загрузки**: MB/минуту в реальном времени
- **ETA**: Оценка времени до завершения

### ⚙️ Гибкая конфигурация
- **Количество потоков**: Настраиваемое количество воркеров
- **Размер очереди**: Контроль памяти
- **Rate limiting**: Настройка лимитов API
- **Таймауты**: Контроль времени ожидания

## 🛠️ Конфигурация

### config.yaml
```yaml
download:
  # Многопоточное скачивание
  concurrent_downloads: 3  # Количество одновременных скачиваний
  max_queue_size: 100     # Максимальный размер очереди
  worker_timeout: 300     # Таймаут воркера (секунды)
  
  # Rate limiting для Telegram API
  rate_limit:
    requests_per_second: 2  # Лимит запросов к Telegram
    burst_size: 5          # Размер burst'а
```

## 🚀 Использование

### Базовое использование
```bash
# Скачивание с 3 потоками (по умолчанию)
python src/main.py

# Переопределить количество потоков
python src/main.py --workers 5

# Ограничить количество файлов
python src/main.py --max-files 50 --workers 3
```

### Мониторинг
```bash
# Показать статистику
python src/main.py --stats

# Показать текущий прогресс (если запущен другой процесс)
python src/main.py --progress
```

### Обслуживание
```bash
# Очистить трекер от несуществующих файлов
python src/main.py --cleanup
```

## 📈 Производительность

### Сравнение скорости
| Режим | Файлов/мин | Ускорение |
|-------|------------|-----------|
| Последовательно | ~5-8 | 1x |
| 3 потока | ~15-20 | 3x |
| 5 потоков | ~20-25 | 4x |

### Рекомендации
- **3 потока**: Оптимально для большинства случаев
- **5 потоков**: Для быстрых соединений
- **2 потока**: Для медленных соединений или строгих лимитов

## 🏗️ Архитектура

### Компоненты системы
```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   Main Parser   │───▶│  Download Queue  │───▶│  Worker Pool    │
│                 │    │                  │    │                 │
│ • Parse messages│    │ • Priority queue │    │ • 3-5 workers   │
│ • Filter media  │    │ • Rate limiting  │    │ • Parallel DL   │
│ • Add to queue  │    │ • Retry logic    │    │ • Error handling│
└─────────────────┘    └──────────────────┘    └─────────────────┘
                                │
                                ▼
                       ┌──────────────────┐
                       │   File Tracker   │
                       │                  │
                       │ • Thread-safe    │
                       │ • Deduplication  │
                       │ • Statistics     │
                       └──────────────────┘
```

### Новые модули
- **`download_queue.py`**: Система очередей с приоритетами
- **`download_worker.py`**: Воркеры и пул потоков
- **`download_coordinator.py`**: Координация процесса
- **`download_monitor.py`**: Мониторинг и статистика

## 🔧 Технические детали

### Rate Limiting
```python
# Автоматическое соблюдение лимитов Telegram
rate_limiter = RateLimiter(
    requests_per_second=2.0,  # 2 запроса в секунду
    burst_size=5              # До 5 запросов в burst
)
```

### Приоритизация
```python
# Меньшие файлы скачиваются первыми
task.priority = int(file_size_mb)  # Приоритет по размеру
```

### Thread Safety
```python
# Все операции с трекерами thread-safe
async with self._lock:
    # Безопасные операции с файлами
```

## 🐛 Отладка

### Логирование
```bash
# Подробные логи
tail -f ./data/logs/downloader.log

# Фильтрация по воркерам
grep "worker_1" ./data/logs/downloader.log
```

### Диагностика
```bash
# Проверка статуса
python src/main.py --stats

# Проверка прогресса
python src/main.py --progress
```

## ⚠️ Ограничения и рекомендации

### Telegram API лимиты
- **Не превышайте 5 потоков** - риск блокировки
- **Соблюдайте rate limits** - 2 запроса/сек рекомендуется
- **Мониторьте ошибки** - при частых ошибках уменьшите потоки

### Системные ресурсы
- **Память**: ~50-100MB на поток
- **Диск**: Одновременная запись нескольких файлов
- **Сеть**: Пропускная способность делится между потоками

### Лучшие практики
1. **Начните с 3 потоков** и увеличивайте постепенно
2. **Мониторьте логи** на предмет ошибок API
3. **Используйте cleanup** регулярно для очистки трекера
4. **Настройте rate limits** под ваше соединение

## 🔄 Миграция с старой версии

Новая версия полностью совместима со старыми конфигурациями. Просто добавьте новые параметры в `config.yaml`:

```yaml
download:
  # Старые параметры остаются
  output_dir: "./data/downloads"
  max_files_per_run: 100
  
  # Новые параметры (опциональные)
  concurrent_downloads: 3
  rate_limit:
    requests_per_second: 2
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в `./data/logs/downloader.log`
2. Попробуйте уменьшить количество потоков
3. Проверьте настройки rate limiting
4. Используйте `--cleanup` для очистки трекера

---

**Наслаждайтесь ускоренным скачиванием! 🎵⚡**